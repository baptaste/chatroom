import React from 'react';

const Form = () => (
  <form>
    <input
      type="text"
      placeholder="Saisissez votre message..."
    />
    <button
      type="button"
    >
      >
    </button>
  </form>
);

export default Form;
